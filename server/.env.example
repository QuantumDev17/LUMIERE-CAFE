NODE_ENV=development
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Postgres
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/lumiere?schema=public

# Email (set DEV_EMAIL_SINK=console to just log emails while developing)
DEV_EMAIL_SINK=console
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Lumière <no-reply@yourdomain.com>"
EMAIL_TO=owner@example.com
